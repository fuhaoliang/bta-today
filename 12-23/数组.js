// arr.splice()闭区间，改变原数组
// arr.slice()左闭右开，不改变原数组
// arr.forEach(value, index, oldArray) 改变原数组
// arr.map(value, index, oldArray)

// arr.find/findIndex
// arr.fill(target, start, end)左闭右开 填充数组
// arr.entries
